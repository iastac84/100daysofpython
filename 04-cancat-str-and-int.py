#!/usr/bin/env python3.6

# Add to integers, then concatenate strings and integers with +
integer1 = 44
integer2 = 22
total = integer1 + integer2

print ("The sum of " + str (integer1) + " and " + str (integer2) + " is " + str (total))  
